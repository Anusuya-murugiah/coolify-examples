services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
  analytics:
    image: nginx
    environment:
      - API_URL="https://openclawroadmap.com/use-cases-development-cicd-integration.php"
